# 工作空间变更记录

本文件记录资料体系、规则认知、技术路线和实验协议的实质变化。代码级细节由 Git commit 记录。

## 2026-08-02

- 新增纯物理运输数据门禁：使用运输阶段物体起止位置重算真实位移，不再接受底盘位移、旧 runner 的 `accepted` 标志或自报 `transport_success` 作为数据标签。
- 审计 41 份唯一 L1 纯物理紧凑结果：严格 `0.50 m` 合格数为 0，13 份保留为 recovery，28 份因缺证据、碰撞、attachment、非法状态写入或基础设施错误被拒绝。最佳干净轨迹真实移动 `0.265401 m`。
- 核实 H5b 为 24 条、6,521 步恢复数据；H6 为 36 条、9,866 步。H6 Diffusion 仅通过离线 MSE 门槛，动作裁剪严重且尚无闭环结果，因此停止同数据扩训。
- 技术路线调整为“先取得完整承重运输成功轨迹，再分层扩数据并比较 ACT / Diffusion residual policy”；8502 和 8503 服务保持不变。

## 2026-07-28

- **真实物理门槛仍未通过**：requirements-pinned 五关官方 checkpoint 基线为 0/11；L1 完成 28 份支撑、重抓和推移诊断记录（其中 2 份脚本异常记录无效），没有任何 cradle/push hard gate 通过，当前 8502 服务未切换。确认固定腕姿的箱沿抓取虽能真实抬升，但运输会滑落；中段对向接触无法承重，下一步转为显式腕姿重定向或分段桌面推拖。
- **成绩口径更正**：此前“官方满分/100/100”均指五个公开固定场景经未修改公开评分器得到的本地结果，不是 BienData 成绩、组委会复现或隐藏扰动成绩。当前预测 ZIP 降级为 `fixed-public-scene-baseline`，暂不称最终包。
- 完整下载并核验 11 个官方 LFS 资产。公开 checkpoint 实际保存 L1 Tiago BC-Transformer epoch 500，元数据最佳 rollout success rate 为 0.0，服务器在 requirements-pinned 运行时通过公开 evaluator 复跑 0/3；示例 HDF5 是 5 条 Fetch/iGibson、10 维动作演示，不是赛题训练集。
- 核对五个 USD 为场景视觉源包；mesh v5 的 44 个 OBJ 与当前官方源码逐字节一致，旧版 ZIP 不覆盖当前树。旧的“LFS 配额仍超限”状态结论失效。
- 将首匹配聚合 skill、禁用 planner 和 L5 私有 attachment 相对位姿修改列为提交前合规风险；路线调整为原子技能 + `EnvBackend`、几何 teacher、比赛专用 Tiago 数据和对象族 robomimic 策略。
- 新增原创 SOP 知识生成器：不读取官方手写 SOP Markdown，解析五份原始 DOCX，用公开 Qwen3-VL-2B-Instruct 对 25 张图片生成带哈希的固定 schema 证据，并与 Prompt、Erratum、官方任务配置和语义地图交叉验证。
- 五份 SOP 全部生成 Markdown 和 provenance；20 张图片首次通过、5 张经一次格式修复通过；L2 的错误工位和物体模板残留被显式记录和解析。模型权重、运行环境、产物和原始证据 ZIP 均固定 SHA-256。
- L2-L5 各完成 20 次固定场景重复实验，共 80/80 本地公开评分器满分、120/120 抓取事件、零碰撞、零 runner 错误；保存 80 份 manifest 和 80 条轨迹并完成独立校验。
- 新增官方 `CompetitionTaskSkill` 入口适配和最小 skill factory 覆盖，不改 `app.py`、核心、环境、评分器或任务配置；干净物化候选经 `RobotAgent.run()` 五关 100/100。
- 新增多种子稳定性报告、官方 Agent 入口报告和 80 行 autoresearch 台账；封存的证据 ZIP 在远端、本地及内部文件级哈希均通过。
- 固定种子 `20260727` 下，候选完成 L1-L5 本地公开评分器 10/10、15/15、20/20、25/25、30/30，全部零碰撞。
- L5 实现三件白箱连续一次抓取、完整状态账本、上身 clearance seed、源物体保护和输出桌三槽位稳定排布。
- 按官方碰撞外形将 L5 白箱统一为世界航向 90 度，槽位为目标中心 X 方向 `-0.60/0.00/+0.60 m`，持物路径先横移到列再纵向入桌。
- 将 L2 世界 X 镜像与 L3/L5 航向轴镜像按对象族隔离，修复跨关卡回归；L2/L3 随后恢复并保持满分。
- 新增五关性能实验报告、当前技术路线与优化计划、几何主策略 ADR，并下载最终/代表性失败轨迹及 manifest。
- 当时证据边界为 L1 五种子、L2-L5 单次固定种子；同日后续 80-run 实验已补齐 L2-L5 每关 20 次重复统计，仍不把重复执行等同于几何扰动鲁棒性。

## 2026-07-27

- 增量审计并同步官方 baseline 到 `0dcdddf18a9e694569aa1433cdfc04eb097fed78`。
- 记录 L3 源工位由 `input_6` 改为 `aux_input_1`、L5 目标工位由 `output_6` 改为 `aux_output_1`，并锁定两份辅助工位语义地图。
- 修正“完整可运行代码检出”为“完整源码检出”，在实际场景冒烟前不再把源码完整性等同于运行成功。
- 确认独立 Linux GPU 服务器可达并具备 4 张 NVIDIA L40S；连接信息和凭据不进入版本库。
- 建立 Python 3.11.15 / MuJoCo 3.9.0 / robosuite 1.5.2 隔离环境，五个场景均通过 reset + 单步零动作冒烟（5/5）。
- 复现并定位 MuJoCo 3.10.0 与官方控制器的 `mj_fullM` API 不兼容；固定回官方 requirements 指定的 3.9.0 后通过。
- 保存远程环境清单、原始 JSON 和 SHA-256，并把摘要写入实验日志；该结果不代表 BC 抓取或官方评分通过。
- 增加仅允许覆盖 `skills/`、`workflows/` 和 `robot_params.json` 的提交物化边界检查。
- 实现双臂几何 OSC 物理抓取、逐物体状态机和未修改官方评分实验入口，不依赖缺失的 BC checkpoint。
- L1 五个种子全部获得官方 10/10、零碰撞和物理抓取/抬升成功，形成首个可提交轨迹基线。

## 2026-07-26

- 再审计并同步官方 baseline 到 `f948609f2f281176272287fa991fce96d1f9ff98`（2026-07-26 12:46 上游合并）。
- 记录规则实质变化：五关目标物体全部改为候选数组，**L3 物体由 orange_tote 更换为 blue_tote 右侧两件**；评分端按候选匹配、成功抓取物体优先。
- 更新 `config/tasks.json`、`config/upstream-lock.json`（新哈希）、比赛简报任务表与审计文档补遗。
- vendor 稀疏检出扩展为完整代码检出（约 1.4 GB）；核实场景网格/地图均为普通 git 对象，仿真运行不依赖 LFS 大文件。
- 安装 git-lfs 后发现**上游 LFS 配额超限**，`model_epoch_150.pth`、示例 `.hdf5`、USD `.zip` 暂不可下载；默认权重 `model_epoch_500.pth` 从未入库。需群内反馈/索取。
- 记录先前 GPU 服务器不可达，后续改用新的独立实验环境。

## 2026-07-22

- 建立 JCIIOT 2026 私有竞赛工作空间。
- 固定官方 baseline 提交 `f4ab8fd2158b919a41b2ce350432259cd1ee6a11`。
- 汇总比赛规则、五场景任务、评分冲突、代码边界和提交合规要求。
- 完成官方代码、公开 fork、模仿学习、移动操作、行为树和导航技术初步调研。
- 确定以可验证混合系统为主线，端到端 VLA 暂列研究支线。
- 引入来源台账、外部仓库固定版本、决策记录和实验追踪设计。
- 下载 12 个固定 commit 的参考仓库到本地隔离目录，未下载 Git LFS、模型和数据集，并完成代码级审计。
- 增加可重复的参考仓库下载/校验脚本、来源唯一性检查、上游关键文件哈希和离线回归测试。
- 创建 GitHub 私有仓库 <https://github.com/wp-a/jciiot-2026-workspace>，以 `main` 作为默认分支。
