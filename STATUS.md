# 当前状态

更新时间：2026-08-02（Asia/Shanghai）

## 当前阶段

报名已完成。五个公开固定场景的旧本地 100/100 基线仍依赖官方 `transport_attachment`，不是纯动力学满分。L1 已追回两条 attachment-free 地面物理推运轨迹：未修改官方 `app._score_steps(0)` 对两条均返回 `10/10`，零碰撞、零物体位姿写入；但机器人使用 direct base-qpos 导航抽象，因此这是“物体真实物理 + 仿真导航”结果，不是完整底盘动力学。下一步把该路线接入官方 `RobotAgent.run()`，再迁移 L2-L5。

## 已完成

- 固定并审计官方仓库提交 `f4ab8fd`（07-22）；07-26 同步至 `f948609`；07-27 增量再审计并同步至 `0dcdddf`。
- 掌握 07-26 规则实质变化：五关候选物体数组、**L3 物体更换为 blue_tote 右侧两件**，工作空间任务事实已同步。
- 掌握 07-27 任务映射变化：L3 源工位改为 `aux_input_1`，L5 目标工位改为 `aux_output_1`，两者中心均为 `[0.144, 8.473]`。
- 核对五个场景、对象、源/目标工位和客观评分口径。
- 保存比赛规则快照与提交合规清单；完成官方 baseline、公开 fork 和相关技术初筛。
- 下载并固定 12 个直接相关的参考仓库，完成关键代码和许可证初审。
- vendor 扩展为完整源码检出（约 1.7 GB）；场景网格和地图是普通 Git 对象。
- 本机安装 git-lfs 与 Python 3.11 Conda 环境（`jciiot`），但仿真依赖尚未补齐，不能标记为可运行环境。
- 已在独立 Linux GPU 服务器建立隔离 Micromamba 环境：Python 3.11.15、MuJoCo 3.9.0、robosuite 1.5.2；硬件为 4 张 NVIDIA L40S、约 125 GB 内存。
- 五个官方场景在固定 seed `20260727` 下全部完成 reset 和一个零动作 step（5/5），action shape 均为 20；结果见 `experiments/2026-07-27-scene-smoke.md`。
- 建立仅覆盖官方允许目录的提交 overlay、物化边界检查、确定性逐物体状态机和官方评分实验入口。
- 实现不写物体位姿的双臂几何 OSC 抓取：只有左右夹爪接触和物理抬升同时成功才记录成功事件。
- L1 在五个种子上全部得到未修改官方评分器 10/10，累计 7,275 轨迹帧零碰撞，最终目标距离均为 0.165 m；详见 `experiments/2026-07-27-l1-scripted-grasp.md`。
- 固定场景候选 `05a7b0d` 在种子 `20260727` 下获得五关公开评分器 100/100、7/7 成功抓取事件、零碰撞；该结果没有提交 BienData，详见 `experiments/2026-07-28-five-level-performance-baseline.md`。
- L2-L5 各完成 20 次独立进程重复实验：80/80 满分、120/120 要求抓取事件、0 碰撞帧、0 runner 错误；每关 Wilson 95% 满分率区间为 `[83.8875%, 100%]`。全部 manifest 和轨迹已封存并独立复核。
- 修复公开 Agent 入口集成缺口：允许目录内的 `CompetitionTaskSkill` 由官方 skill factory 首位注册，使用 planner feature gate 进入确定性流程；干净候选通过 `RobotAgent.run()` 五关公开评分器 100/100 验收。
- 下载并逐项哈希核验官方 11 个 Git LFS 资产；权重、示例 HDF5、五个 USD 和四个 mesh ZIP 均与 LFS 对象及用户给定大小一致。
- 安全解析公开 checkpoint：实际为 L1 Tiago 20 维 BC-Transformer、epoch 500，保存的最佳 rollout success rate 为 0.0；服务器在完全匹配核心 requirements 的隔离环境通过未修改公开抓取 evaluator 重复 3 次均失败。
- 审计 591 MB HDF5：仅含 5 条 Fetch/iGibson 厨房整理演示，action shape 为 10，与 JCIIOT Tiago 的 20 维动作不兼容，只能作为格式样例。
- 完成五关官方 checkpoint 的 seed 0 真实抓取基线：11 个可配置物体尝试中 0/11 通过物理抓取门槛；两次碰撞均来自 L3 辅助工位，L1 far 物体缺少双侧 grasp site。
- L1 完成 28 份支撑、重抓和推移诊断记录，其中两份脚本异常记录明确标为无效；其余实验均未通过 cradle 或 push hard gate，当前 8502 服务没有切换。
- 证明 L1 官方端壁抓取能真实接触并抬升，但长距离移动发生滑落；箱体中段对向实验能获得瞬时双臂 fingertip 接触，却无法承重抬升。固定腕姿的位置参数扫描已终止，后续必须显式重定向 Robotiq 闭合轴或改用分段桌面推拖。
- 核对 mesh 历史包：当前官方源码中的 44 个相关 OBJ 与 v5 包逐字节一致，无需覆盖解压；USD 仅作为场景/可视化源资产归档。
- 证明候选的 `app.py`、`task_config.json` 和 `task_subprocess_runner.py` 与锁定官方源码 SHA-256 一致；服务器无可访问 X display，图形按钮/可见 viewer 本身未在该机验证。
- 实现原创 SOP 生成器：不读取官方手写 SOP Markdown，确定性解析五份原始 DOCX，并用公开 Qwen3-VL-2B-Instruct 离线生成带哈希的图片证据；5/5 文档和 25/25 图片通过 schema 与官方配置/语义地图交叉检查。
- 显式检出 L2 正文模板中的两项冲突并按 Prompt 与官方 Erratum 解析；固定模型权重和运行环境，保存五份 Markdown、五份 provenance、manifest 与 checksums。
- L5 建立三对象完整状态账本、连续抓取上身安全位、源物体保护和输出桌三槽位排布；三件最终距离为 0.60/0.00/0.60 m。
- L2/L3 tote 镜像按对象族隔离：世界对齐工位使用世界 X 轴，旋转工位使用航向轴，避免跨关卡回归。
- 下载最终五关 manifest/轨迹、L5 全部迭代 manifest 和代表性失败轨迹到本地忽略目录，并记录关键 SHA-256。
- 创建 GitHub 私有仓库 `wp-a/jciiot-2026-workspace`，远程默认分支为 `main`。
- H2 收集 14 条成功抓取轨迹；实际进入冻结模型划分的 12 条与 H5b 的 24 条恢复轨迹合并为 H6 数据集，共 36 条、9,866 步，动作维度 20。
- H3 BC-RNN 闭环 0/5，H4 Diffusion 闭环 2/5，均未达到晋级门槛。H6 epoch 140 的离线中位 MSE 为 0.029003，但三次采样分别有 477/599/530 个动作步被裁剪，尚未执行有效闭环晋级测试。
- 建立 fail-closed 纯物理数据门禁并审计 41 份唯一 L1 结果：0 条满足真实物体位移 0.50 m，13 条可作 recovery，28 条拒绝。原标记为“最佳干净”的 `0.265401 m` 记录后来确认使用 direct base-qpos 步进，只能证明无 attachment 的接触运输，不能证明完整机器人动力学。
- 完成一次预注册的单变量物理运输实验：仅把运行器内部漂移保护从 0.03 m 改为 0.04 m，物体位移提高至 0.275275 m，但出现 2 帧躯干与生产线碰撞，因此改动和数据均被严格拒绝。该实验确认下一瓶颈是路径/承重姿态碰撞余量。
- 完成 L1 世界 `+x` 反向撤离诊断：生产线碰撞降为 0，但右侧物理接触在 57 步后丢失，底盘/物体分别移动 0.107013/0.123148 m，最终最小抬升仅 0.026333 m。零 attachment、零状态写入，数据归入 recovery，不计成功。
- 完成 L1 `+x` 反向撤离内收保持消融：只增加每步 `0.0005 m` 双臂内收后，平面漂移略降至 `0.028135 m`，但右侧接触提前 3 步丢失，真实物体位移降至 `0.116922 m`。该参数已弃用；控制器停止前物体高度下降约 `0.069 m`，下一单变量转向 actuator-only 的连续垂直承重。
- 完成 L1 `+x` 反向撤离垂直承重消融：有界垂直保持把终止状态改为双侧接触，但 54 步后仍触发 `0.030096 m` 平面漂移保护。控制期夹爪前进约 `0.105-0.109 m`，箱体只前进约 `0.084 m`，证明当前同时移动底盘的侧壁夹持是摩擦拖拽，继续调整夹紧、抬升或漂移阈值不会解决根因。
- 完成 L1 纯 composite-action 纠错实验：原始 attachment 配置错误明确排除；修正运行真实抓取成功、零碰撞且零 attachment/object-pose/robot-state 写入，但底盘 635 步仅移动 `0.002161 m`，箱体跌回源支撑并向错误方向滑移，路线严格 rejected。完整证据见 `autoresearch/classic-260802-l1-exposed-edge-setdown/`。
- 追回并独立复核 07-31 floor-push 结果：`iter33/34` 均有真实 `grasp_end`，经 6,985 个接触推运步骤到目标 `0.748201 m`，14,299 帧零碰撞、零 attachment、零物体位姿写入；锁定官方 scorer 对两条均返回 `10/10`。证据见 `autoresearch/classic-260802-supported-transport-inventory/`。

## 当前结论

主线采用可验证混合架构，并严格区分“官方可用导航抽象”和“完整纯动力学”。L1 实用基线现为真实抓取、物理 setdown、地面接触推运和官方 scorer `10/10`；底盘 direct-qpos 必须在报告中显式声明。BC-RNN / Diffusion 同数据扩训继续暂停，因为它们没有改善完整任务。当前工程目标不是再调 L1 推运参数，而是把已验证路线接入允许目录内的官方 Agent 入口，并保持同一评分和完整性证据。

## 阻塞与风险

- 上游 11 个 LFS 对象目前均可下载，旧的“配额仍超限”结论已经失效；后续仍需按锁定 SHA-256 缓存，避免上游再次不可用。
- L2-L5 已通过每关 20 次重复执行，但 seed 未改变轨迹帧数和最终几何，不能把该结果表述为初始位姿或动力学扰动鲁棒性。
- 当前 `competition_task` 首匹配并禁用 planner，虽然可复现，但与培训材料展示的 LLM plan + 原子技能链不完全一致；需取得组委会澄清或恢复可验证原子技能轨迹。
- L5 放置会修改 transport attachment 的相对状态并调用私有同步帮助函数。它不直接写物体 qpos，但存在合规和评委观感风险，最终提交前必须改成真实底盘/机械臂动作或获得明确许可。
- 公开 checkpoint 在 requirements-pinned L1 evaluator 上稳定失败；示例 HDF5 又不是比赛数据，不能依赖官方资产替代自采 Tiago 演示。
- L5 约 281.5 秒，可靠性已达标但效率仍有优化空间；缩短等待必须以满分和零碰撞为约束。
- 当前创新以系统集成为主，SOP 原创证据链已完成，仍需用模块消融和失败统计增强 40% 创新论证。
- L1 固定腕姿位置扫描已经被物理实验否决；腕姿旋转约束、关节可达性和旋转过程碰撞风险尚未验证。
- VLM 图片描述可能为空或不精确，不能作为任务真值；当前实现只把它作为离线证据，任务合同仍由 Prompt、Erratum、官方配置和语义地图交叉验证。
- 公开同赛题 fork 的自报结果尚未在未修改官方评分器上独立复现。
- 评测服务器、联网策略、隐藏扰动和单关时间限制仍待组委会确认。
- MimicGen、AnyGrasp 等组件存在授权或使用范围风险，当前禁止直接集成。
- 当前 41 条纯物理诊断没有一条达到 `0.50 m` 真实物体位移；旧 `0.25 m` accepted 记录的物体实际只移动 `0.199200 m`。
- H6 仅有离线结果，且动作裁剪比例高；在获得正确运输数据之前不进行更大模型或更多 epoch 训练。
- 锁定环境的 Tiago wheel-action 控制器不足以完成跨场景导航；direct-base qpos 导航可以移动机器人，但不能再标为完整纯动力学。若比赛允许它作为官方导航抽象，只能单独形成“官方接口合规 + 物体真实接触”路线并在报告中显式声明。
- `iter33/34` 由研究 runner 生成，尚未证明当前 8502 的 `RobotAgent.run()` 会选择 floor-push 路线；在完成官方入口复现前不能作为最终提交候选。

## 下一里程碑

1. 将冻结 `iter33` 路线接入允许修改的 `skills/` / `workflows/`，不改 core、environment、`app.py` 或 `task_config.json`。
2. 通过一次官方 `RobotAgent.run()` L1 入口，要求 scorer `10/10`、零碰撞、零 attachment / object-pose write；此前不做多 seed。
3. 复用同一接触拓扑迁移 L2，再按关卡顺序推进 L3-L5；每关只跑一个预注册必要实验。
4. 五关单 seed 通过后再做最小必要鲁棒性验证、最终 ZIP 和技术报告。

## 更新规则

- 本文件只写当前状态和最近动作，不存放长篇研究结论。
- 规则事实更新到 `docs/00-competition-brief.md`，技术结论更新到对应专题文档。
- 技术路线变化必须新增 `decisions/` 记录；实验结论必须链接 `experiments/experiment-log.csv` 中的运行。
- 每次状态更新同时在 `CHANGELOG.md` 留下日期和摘要。
