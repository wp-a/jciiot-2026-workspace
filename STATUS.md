# 当前状态

更新时间：2026-07-26（Asia/Shanghai）

## 当前阶段

报名已完成，距作品提交截止（2026-08-16 23:59）约 21 天。调研与工作空间治理已就绪，**自研实现尚未开始**（`src/` 为空、实验日志为空）。当前主线不变：固定官方基线、建立 L1 可复现闭环、再分模块优化。

## 已完成

- 固定并审计官方仓库提交 `f4ab8fd`（07-22）；07-26 增量再审计并同步至 `f948609`。
- 掌握 07-26 规则实质变化：五关候选物体数组、**L3 物体更换为 blue_tote 右侧两件**，工作空间任务事实已同步。
- 核对五个场景、对象、源/目标工位和客观评分口径。
- 保存比赛规则快照与提交合规清单；完成官方 baseline、公开 fork 和相关技术初筛。
- 下载并固定 12 个直接相关的参考仓库，完成关键代码和许可证初审。
- vendor 扩展为完整可运行代码检出（约 1.4 GB）；核实仿真运行不依赖被 LFS 卡住的大文件。
- 本机安装 git-lfs 与 Python 3.11 conda 环境（`jciiot`），用于本地冒烟验证。
- 创建 GitHub 私有仓库 `wp-a/jciiot-2026-workspace`，远程默认分支为 `main`。

## 当前结论

主线采用混合架构：SOP 多模态结构化、确定性任务编排、风险感知导航、robomimic 局部抓取、物理放置和显式失败恢复。先优化可靠性和零碰撞，再优化时间；学习方法只替换已证明是瓶颈的局部模块。候选物体机制新增一个合规差异化点：官方默认取第一个候选，可按可达性/抓取可靠性做候选选择。

## 阻塞与风险

- **上游 LFS 配额超限**：`model_epoch_150.pth`、示例 `.hdf5`、USD `.zip` 从 GitHub 拉取失败；默认权重 `model_epoch_500.pth` 从未入库。需官方 QQ 群反馈并索取权重，或按官方脚本自采数据训练 BC。
- **GPU 训练环境不可达**：myserver（211.87.224.135:19222）直连超时、经本地代理 CONNECT 被拒，疑需校园网/VPN。恢复前无法启动训练类实验。
- 距截止仅 3 周而实现为零：L1 闭环若一周内无法跑通，需收缩创新面、优先保客观分。
- 公开同赛题 fork 的自报结果尚未在未修改官方评分器上独立复现。
- 评测服务器、联网策略、隐藏扰动和单关时间限制仍待组委会确认。
- MimicGen、AnyGrasp 等组件存在授权或使用范围风险，当前禁止直接集成。

## 下一里程碑

1. 本机完成 `test_scene_load.py` 冒烟验证，确认五场景资产完整可加载。
2. 恢复 myserver（或替代 GPU 环境），部署完整 baseline。
3. 取得/训练 BC 权重；固定计划完成 `move -> pick -> move -> place`，产生真实成功抓取事件、零碰撞和官方 10/10。
4. 以至少 5 个固定种子建立 baseline 成功率和失败分类。
5. 依次优化导航安全、抓取数据闭环和 L5 三物体 workflow。

## 更新规则

- 本文件只写当前状态和最近动作，不存放长篇研究结论。
- 规则事实更新到 `docs/00-competition-brief.md`，技术结论更新到对应专题文档。
- 技术路线变化必须新增 `decisions/` 记录；实验结论必须链接 `experiments/experiment-log.csv` 中的运行。
- 每次状态更新同时在 `CHANGELOG.md` 留下日期和摘要。
